
using System;

class Student
{
    private string name;
    private int age;

    public string Name
    {
        get { return name; }
        set
        {
            if (!string.IsNullOrWhiteSpace(value))
                name = value;
            else
                Console.WriteLine("Invalid name!");
        }
    }

    public int Age
    {
        get { return age; }
        set
        {
            if (value > 0)
                age = value;
            else
                Console.WriteLine("Invalid age!");
        }
    }

    public void Display()
    {
        Console.WriteLine("Student Name: " + Name);
        Console.WriteLine("Student Age: " + Age);
    }
}

class Person
{
    private string place;

    public string Place
    {
        get { return place; }
        set
        {
            if (!string.IsNullOrWhiteSpace(value))
                place = value;
            else
                Console.WriteLine("Invalid place!");
        }
    }

    public void Display()
    {
        Console.WriteLine("Place: " + Place);
    }
}

class BankAccount
{
    private double balance;

    public double Balance
    {
        get { return balance; }
        private set { balance = value; }
    }

    public void Deposit(double amount)
    {
        if (amount > 0)
        {
            Balance += amount;
            Console.WriteLine("Deposited Amount: " + amount);
        }
        else
        {
            Console.WriteLine("Invalid deposit amount!");
        }
    }

    public void Withdraw(double amount)
    {
        if (amount > 0 && amount <= Balance)
        {
            Balance -= amount;
            Console.WriteLine("Withdrawn Amount: " + amount);
        }
        else
        {
            Console.WriteLine("Insufficient Balance or Invalid Amount!");
        }
    }

    public void Show()
    {
        Console.WriteLine("Current Balance: " + Balance);
    }
}

class Program
{
    static void Main()
    {
        Console.WriteLine("----------- Student Example -----------");
        Student s1 = new Student();
        s1.Name = "kaviya";
        s1.Age = 23;
        s1.Display();

        Console.WriteLine("\n----------- Person Example -----------");
        Person p1 = new Person();
        p1.Place = "Chennai";
        p1.Display();

        Console.WriteLine("\n----------- Bank Account Example -----------");
        BankAccount bank = new BankAccount();
        bank.Deposit(5000);
        bank.Withdraw(2000);
        bank.Show();
    }
}


--------------------------------------------------------------------------

expected output:

----------- Student Example -----------
Student Name: kaviya
Student Age: 23

----------- Person Example -----------
Place: Chennai

----------- Bank Account Example -----------
Deposited Amount: 5000
Withdrawn Amount: 2000
Current Balance: 3000


--------------------------------------------------------------------------------
Improvements Achieved:

Implemented encapsulation by converting public fields into private fields with proper getter setter validation methods.

Replaced traditional getter/setter methods with C# properties for cleaner and modern code design.

Applied input validation (e.g., age and transaction amounts) to ensure data integrity.

Improved method naming conventions using PascalCase (e.g., Display, Deposit, Withdraw).

Ensured data security by restricting direct access to sensitive fields like balance.

Structured code into separate logical classes (Student, Person, BankAccount) for better readability.

Enhanced console output formatting with clear section-wise examples.

Fixed logical issues (e.g., incorrect message in Withdraw method)



